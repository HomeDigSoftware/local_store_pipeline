# הצעה: שיפור מציאותיות הנתונים הסינתטיים
**תאריך:** 2026-06-10  
**קובץ בשימוש:** `scripts/generate_synthetic_invoices_03.py`  
**קובץ הגדרות:** `scripts/02_extract_load_03.py`

---

## הבעיה הנוכחית

הגרף נראה "שטוח מדי" ולא אמיתי. הסיבות:

| בעיה | קוד נוכחי | השפעה |
|------|-----------|--------|
| **seed קבוע** | `SYNTHETIC_SEED = 42` | כל יום משתמש **באותה רצף אקראי** — הנתונים דומים מאוד יום לאחר יום |
| **ג'יטר קטן מדי** | `rng.uniform(0.92, 1.08)` | שינוי מקסימלי של ±8% בלבד סביב הממוצע |
| **אין מגמה** | `base_count = 105` קבוע | הגרף "שוכן" סביב אותו מספר — אין עלייה לאורך זמן |
| **אין אירועים** | — | אין ימים חזקים/חלשים בצורה בולטת |

---

## הדגמה — מה קורה כיום לעומת מה שרצוי

### כיום (ג'יטר ±8% + seed קבוע):
```
יום 1 (ראשון)  : 132 חשבוניות
יום 2 (שני)    : 120 חשבוניות
יום 3 (שלישי) : 127 חשבוניות
יום 4 (רביעי) : 134 חשבוניות
יום 5 (חמישי) : 142 חשבוניות
יום 6 (שישי)  :  79 חשבוניות
יום 7 (שבת)   :  79 חשבוניות
              ──────────────────
              נראה: קו ישר עם גל שבועי. לא אמיתי.
```

### לאחר השינוי (הצעה מומלצת):
```
שבוע 1:   132 | 98 | 141 | 167 | 88 | 61 | 72
שבוע 10:  155 | 112 | 178 | 145 | 94 | 70 | 88
שבוע 30:  198 | 145 | 220 | 191 | 78 | 88 | 105
          ──────────────────────────────────────
          נראה: עלייה כללית + שונות גדולה + ימי שיא ושפל
```

---

## שלוש אפשרויות

---

### אפשרות A — מינימלית (שינוי אחד פשוט)
**"seed לפי תאריך + ג'יטר גדול יותר"**

**שינויים ב-`02_extract_load_03.py`:**
```python
# לפני:
SYNTHETIC_SEED = 42

# אחרי:
SYNTHETIC_SEED = None   # None = seed שונה בכל הרצה (או seed לפי תאריך — ראה הערה)
```

**שינוי ב-`generate_synthetic_invoices_03.py`:**
```python
# לפני:
jitter = rng.uniform(0.92, 1.08)

# אחרי:
jitter = rng.uniform(0.70, 1.40)   # ±30-40% שונות
```

**תוצאה:**
- שינוי פשוט מאוד
- כל יום יהיה שונה באמת
- עדיין אין מגמה עולה
- ±30-40% שונות בין ימים — מורגש בגרף

**מתי לבחור:** רוצה שינוי מינימלי שיעשה הבדל מיידי.

---

### אפשרות B — בינונית (מומלצת) ✅
**"מגמת צמיחה + ג'יטר לוג-נורמלי"**

**רעיון:**
- כמות החשבוניות הבסיסית גדלה ב-0.08% ביום (≈25% בשנה) — מגמה עולה אך לא לינארית
- ג'יטר לוג-נורמלי (sigma=0.18) — יוצר ימים חזקים ממוצע + מדי פעם שיא גדול
- seed לפי תאריך — שונה בכל יום אך **ניתן לשחזור** (רץ פעמיים על אותו תאריך = אותה תוצאה)

**שינויים ב-`02_extract_load_03.py`:**
```python
SYNTHETIC_INVOICE_COUNT  = 90     # base נמוך יותר — הצמיחה תיקח אותו ל-105+ עם הזמן
SYNTHETIC_SEED           = None   # date-based seed יחושב בתוך generate_synthetic_invoices_03.py
TREND_ANCHOR_DATE        = date(2021, 3, 26)
TREND_DAILY_GROWTH_RATE  = 0.0008   # 0.08% ליום ≈ 25% לשנה
```

**שינוי ב-`generate_synthetic_invoices_03.py` — פונקציה `adjusted_invoice_count`:**
```python
def adjusted_invoice_count(
    base_invoice_count: int,
    target_date: date,
    rng: random.Random,
    trend_anchor: date | None = None,
    growth_rate: float = 0.0,
) -> int:
    profile = resolve_day_profile(target_date)

    # מגמה: כל יום קצת יותר
    if trend_anchor is not None:
        days_elapsed = max(0, (target_date - trend_anchor).days)
        trend_factor = (1 + growth_rate) ** days_elapsed
    else:
        trend_factor = 1.0

    # ג'יטר לוג-נורמלי: sigma=0.18 נותן CV של ~18%
    # מדי פעם ייצר יום שיא של 1.5×-2× הממוצע
    import math
    sigma = 0.18
    lognormal_jitter = math.exp(rng.gauss(0, sigma))   # ≈ 0.7 עד 1.5 ב-95% מהמקרים

    count = base_invoice_count * profile.invoice_multiplier * trend_factor * lognormal_jitter
    return max(1, round(count))
```

**seed לפי תאריך (מוסף לתחילת `main()`):**
```python
# seed ייחודי לכל תאריך אך ניתן לשחזור
if args.seed is None:
    date_seed = int(target_date.strftime("%Y%m%d"))
    rng = random.Random(date_seed)
else:
    rng = random.Random(args.seed)
```

**תוצאה מדומה (base=90, growth=0.08%/day):**
```
תאריך      | ימים מ-anchor | trend×   | jitter | count
---------- | ------------- | -------- | ------ | -----
2021-03-26 |    0          | 1.00     | 0.95   |  85
2021-06-01 |   67          | 1.054    | 1.18   | 112
2022-01-01 |  281          | 1.252    | 0.87   | 122
2023-01-01 |  646          | 1.668    | 1.31   | 196
2024-01-01 | 1012          | 2.234    | 0.79   | 200
2025-01-01 | 1377          | 3.006    | 1.42   | 342  ← שיא
2025-06-01 | 1528          | 3.404    | 0.93   | 357
```

**מתי לבחור:** רוצה גרף שנראה אמיתי ומציג סיפור עסקי — עסק שצומח.

---

### אפשרות C — מלאה (הכי מציאותי) ✅✅
**"מגמה + ג'יטר + 4 שכבות אירועים"**

כל מה שב-B, בתוספת 4 שכבות אירועים:

---

#### שכבה 1: אפקט תחילת חודש (ימים 1–3)
```python
# ימים 1-3 לחודש: לקוחות רק קיבלו משכורת → מחזור גבוה
if target_date.day <= 3:
    return rng.uniform(1.25, 1.60)
```

---

#### שכבה 2: ⭐ חדש — "שבוע לפני משכורת" (ימים 4–9)
```python
# ימים 4-9 לחודש: הכסף מהמשכורת כבר אזל → קונים פחות
# אנשים מקצצים בהוצאות — מחזור נמוך יותר
if 4 <= target_date.day <= 9:
    return rng.uniform(0.62, 0.82)   # -18% עד -38% מהממוצע
```

**הלוגיקה:**  
ימים 1-3 = משכורת נכנסת → קנייה מוגברת  
ימים 4-9 = כסף הולך ונגמר → שפל  
ימים 10-31 = חזרה לרגיל (עד המשכורת הבאה ב-15)

---

#### שכבה 3: אפקט אמצע חודש (יום 15)
```python
# יום 15 לחודש: משכורת שניה / העברות → עלייה מתונה
if target_date.day == 15:
    return rng.uniform(1.15, 1.45)
```

---

#### שכבה 4: ⭐ חדש — "יום הפתעה" אחת ל-3 עד 4 שבועות
**הבעיה עם גישה פשוטה:**  
אם נכתוב `if rng.random() < 0.05: surprise`, כל יום יש 5% סיכוי. זה לא "הפתעה" — זה רעש. הפתעה אמיתית = קורה לעתים רחוקות אך **בצורה בלתי צפויה לחלוטין**.

**הפתרון — "חלון אקראי":**  
במקום הסתברות יומית, קובעים מראש **יום מסוים** בתוך חלון של 21-28 ימים.  
הבחירה נעשית לפי seed שמבוסס על מספר החלון — כך **ניתן לשחזור** אך לא ניתן לנחש.

```python
SURPRISE_ANCHOR = date(2021, 3, 26)   # anchor date של הפרויקט

def _surprise_event_multiplier(target_date: date) -> float:
    """
    מחשב אם היום הוא "יום הפתעה".
    
    הלוגיקה:
    1. מחלקים את כל הזמן לחלונות של 21-28 ימים
    2. בכל חלון קובעים מראש יום אחד שיהיה ה"הפתעה"
    3. הבחירה נעשית לפי seed = מספר החלון (ניתן לשחזור)
    4. הסוג (חזק/חלש) גם הוא נקבע לפי אותו seed
    """
    days_from_anchor = (target_date - SURPRISE_ANCHOR).days
    if days_from_anchor < 0:
        return 1.0

    # גודל החלון נקבע לפי seed של מספר החלון הנוכחי
    # — אפילו אורך החלון עצמו אקראי (21 עד 28 ימים)
    # מחשבים איזה חלון אנחנו בו באיטרציה
    window_rng = random.Random(days_from_anchor // 21)   # seed = מספר חלון בקירוב
    window_size = window_rng.randint(21, 28)             # 21–28 ימים באקראי
    surprise_day_offset = window_rng.randint(0, window_size - 1)  # יום ספציפי בחלון
    is_strong = window_rng.random() < 0.60               # 60% הפתעה חיובית, 40% שלילית

    # נחשב בדיוק את גבולות החלון
    window_number = days_from_anchor // window_size
    window_start_day = window_number * window_size
    surprise_day_abs = window_start_day + surprise_day_offset

    if days_from_anchor == surprise_day_abs:
        if is_strong:
            return window_rng.uniform(1.65, 2.50)   # יום שיא: +65% עד +150%
        else:
            return window_rng.uniform(0.20, 0.45)   # יום נפילה: -55% עד -80%

    return 1.0   # יום רגיל
```

**למה זה "הפתעה אמיתית":**
- לא תוכל לנחש מתי הוא יבוא — גם לא 21, 22, 23 או 28 ימים מהאחרון
- גם לא תדע אם הוא חיובי או שלילי עד שמגיעים לתאריך
- הוא עדיין **ניתן לשחזור** — backfill פעמיים יתן אותה תוצאה

---

**פונקציה מרכזת — `_monthly_event_multiplier`:**
```python
def _monthly_event_multiplier(target_date: date, rng: random.Random) -> float:
    day = target_date.day

    # שכבה 1: תחילת חודש (משכורת נכנסת)
    if day <= 3:
        return rng.uniform(1.25, 1.60)

    # שכבה 2: שבוע לפני משכורת (כסף אוזל)
    if 4 <= day <= 9:
        return rng.uniform(0.62, 0.82)

    # שכבה 3: אמצע חודש (משכורת שניה)
    if day == 15:
        return rng.uniform(1.15, 1.45)

    # שכבה 4: יום הפתעה (אחת ל-3-4 שבועות, אקראי לחלוטין)
    surprise = _surprise_event_multiplier(target_date)
    if surprise != 1.0:
        return surprise

    return 1.0
```

---

**תוצאה לדוגמה — חודש יוני מלא:**
```
תאריך | יום  | מכפיל    | count | הסבר
------ | ---- | -------- | ----- | ------
01/06  | א'   | 1.42     |  312  | 📈 תחילת חודש (שכבה 1)
02/06  | ב'   | 1.31     |  289  | 📈 תחילת חודש (שכבה 1)
03/06  | ג'   | 1.18     |  260  | 📈 תחילת חודש (שכבה 1)
04/06  | ד'   | 0.74     |  163  | 📉 שבוע לפני משכורת (שכבה 2)
05/06  | ה'   | 0.81     |  178  | 📉 שבוע לפני משכורת
06/06  | ו'   | 0.68     |   84  | 📉 שישי × pre-pay
07/06  | ש'   | 0.69     |   85  | 📉 שבת × pre-pay
08/06  | א'   | 0.77     |  169  | 📉 שבוע לפני משכורת
09/06  | ב'   | 0.71     |  157  | 📉 שבוע לפני משכורת (שכבה 2)
10/06  | ג'   | 1.05     |  231  | רגיל
11/06  | ד'   | 0.94     |  207  | רגיל
12/06  | ה'   | 1.18     |  260  | רגיל
13/06  | ו'   | 0.88     |  109  | שישי רגיל
14/06  | ש'   | 0.79     |   97  | שבת רגיל
15/06  | א'   | 1.38     |  304  | 📈 משכורת שניה (שכבה 3)
16/06  | ב'   | 1.11     |  244  | רגיל
17/06  | ג'   | 2.18     |  480  | 💥 יום הפתעה חזק (שכבה 4)
18/06  | ד'   | 0.96     |  211  | רגיל
...    | ...  | ...      |  ...  |
26/06  | ו'   | 0.33     |   40  | 💤 יום הפתעה חלש (שכבה 4)
```

**מתי לבחור:** רוצה נתונים שנראים כמו חנות אמיתית — עם מחזוריות חודשית ברורה ושיאים/שפלים בלתי צפויים.

---

## המלצה

**אפשרות C** — הטובה ביותר עבור הפרויקט (עם כל 4 השכבות):

| קריטריון | A | B | C |
|---------|---|---|---|
| פשטות יישום | ✅✅ | ✅ | ⚠️ |
| שונות יום-יומית | ✅ | ✅✅ | ✅✅✅ |
| מגמה עולה | ❌ | ✅✅ | ✅✅ |
| מחזוריות חודשית | ❌ | ❌ | ✅✅✅ |
| שפל "לפני משכורת" | ❌ | ❌ | ✅✅✅ |
| הפתעות בלתי צפויות | ❌ | ❌ | ✅✅✅ |
| נראה אמיתי בגרף | ✅ | ✅✅ | ✅✅✅ |
| ניתן לשחזור (backfill) | ❌* | ✅ | ✅ |
| אפקט על KPIs בדשבורד | קטן | גדול | גדול מאוד |

> *אפשרות A עם seed=None תיצור backfill שונה בכל הרצה

---

## מה צריך לשנות (אפשרות C — 4 שכבות)

**קובץ 1:** [02_extract_load_03.py](scripts/02_extract_load_03.py)
- `SYNTHETIC_INVOICE_COUNT = 90` (היה 105)
- `SYNTHETIC_SEED = None` (היה 42)
- להוסיף: `TREND_ANCHOR_DATE` ו-`TREND_DAILY_GROWTH_RATE`
- להעביר את הערכים החדשים ל-command שמפעיל את generate_synthetic_invoices_03.py

**קובץ 2:** [generate_synthetic_invoices_03.py](scripts/generate_synthetic_invoices_03.py)
- לשנות פונקציה `adjusted_invoice_count` — להוסיף `trend_factor` + lognormal jitter
- להוסיף פונקציה `_surprise_event_multiplier` — מנגנון ה"הפתעה" האקראית
- להוסיף פונקציה `_monthly_event_multiplier` — כל 4 השכבות החודשיות
- לשנות `main()` — seed לפי תאריך כשאין `--seed`
- להוסיף 2 ארגומנטים ל-`parse_args()`: `--trend-anchor-date`, `--trend-growth-rate`

**סה"כ: ~55 שורות קוד בשני קבצים.**

---

## סיכום כל השכבות — לוגיקת העדיפויות

```
כל יום מחושב כך:

base_count × weekday_multiplier × trend_factor × lognormal_jitter × event_multiplier

event_multiplier נקבע לפי סדר עדיפויות:
  1. יום 1-3 לחודש    → ×1.25–1.60  (משכורת נכנסת)
  2. יום 4-9 לחודש    → ×0.62–0.82  (שבוע לפני משכורת — ⭐ חדש)
  3. יום 15 לחודש     → ×1.15–1.45  (משכורת שניה)
  4. יום הפתעה         → ×1.65–2.50 או ×0.20–0.45 (אחת ל-3-4 שבועות — ⭐ חדש)
  5. יום רגיל          → ×1.00
```

---

## השפעה על הגרף לאחר השינוי

```
ציר Y: מכירות יומיות (₪)
^
|                                              . .
|                               . .          .   .
|                    . .       .   .  . .   .
|            . .    .   .  .  .         . .
|    . .    .   . .     . .
|   .   . .
|
+---------------------------------------------------> ציר X: תאריך
2021                  2023                  2025-2026

מגמה עולה ברורה + שינוי יומי מורגש + מדי פעם שיאים בולטים
```

---

## ✅ עודכן בקוד — 2026-06-10

הוטמעה אפשרות C בפועל בשני הקבצים.  
ההבדל היחיד מהמוצע לעיל: ימי 1-3 לא קיבלו בוסט (לוח שכר ישראלי = ה-10 לחודש),  
במקומם: **ימים 10-12 = בוסט × 1.20–1.45** | **ימים 4-9 = שפל × 0.62–0.82**

---

---

## ⚠️ בעיה חדשה — כיול מחזור יומי (Revenue Calibration)

**תאריך:** 2026-06-10

### הבעיה

נצפים ימים עם מחזור 15,000+ ₪, שאינו ריאליסטי לחנות נוחות קטנה.  
**גבולות רצויים:** 5,500–11,000 ₪ ביום רגיל, מקסימום 12,000 ₪ בימים חריגים מאוד.

### ניתוח שורש הבעיה — מכפילים שמצטברים

עם הפרמטרים המיושמים (`base=105`, `rate=0.0003`, `surprise ×2.50`):

```
בתאריך 2026 (5 שנים מ-anchor):
  trend factor = (1.0003)^1900 ≈ ×1.77

יום ראשון + ג'יטר גבוה + יום הפתעה חזק:
  105 × 1.15 (Sun) × 1.77 (trend) × 1.5 (jitter) × 2.50 (surprise)
= 105 × 1.15 × 1.77 × 3.75
≈ 800 חשבוניות!

ממוצע חשבונית ≈ 40–47 ₪  →  800 × 42 ≈ 33,600 ₪ 🚨

אפילו ביום רגיל (יום משכורת, ג'יטר ×1.4):
  105 × 1.15 × 1.77 × 1.4 × 1.45 ≈ 434 חשבוניות ≈ 18,228 ₪
```

**שלושה גורמים מתחזקים זה את זה בלי תקרה:**
1. `base=105` גבוה מדי
2. `trend ×1.77` בעוד 5 שנים — כבר מכפיל ב-77% את הבסיס
3. `surprise ×2.50` — אין תקרה שמגנה מקומבינציה

---

### הפתרון — 5 שינויים קטנים + תקרה

| פרמטר | כרגע | מוצע | אפקט |
|--------|------|------|------|
| `SYNTHETIC_INVOICE_COUNT` | 105 | **85** | בסיס קטן יותר ב-19% |
| `TREND_DAILY_GROWTH_RATE` | 0.0003 | **0.00015** | ×1.32 ב-5 שנים (במקום ×1.77) |
| Surprise חזק | ×1.65–2.50 | **×1.35–1.65** | שיא ריאליסטי לחנות נוחות |
| Surprise חלש | ×0.20–0.45 | **×0.30–0.52** | ירידה בולטת אך לא קריסה |
| Payday boost (10-12) | ×1.20–1.45 | **×1.10–1.30** | בוסט עדין יותר |
| תקרה קשיחה | אין | **260 חשבוניות** | מגן מקומבינציות קיצוניות |

---

### אימות — מחזור צפוי לאחר תיקון

**הנחה:** ממוצע חשבונית ≈ 47 ₪ (1–4 מוצרים × 15–60 ₪ בחנות נוחות)

**2021 (תחילת הנתונים):**

| תרחיש | חשבוניות | מחזור |
|--------|----------|-------|
| ראשון רגיל | 85 × 1.15 × 1.00 = 98 | 4,606 ₪ |
| ד' רגיל | 85 × 1.10 × 1.00 = 94 | 4,418 ₪ |
| שישי/שבת | 85 × 0.60 × 1.00 = 51 | 2,397 ₪ |

**2026 (אחרי 5 שנים, trend ×1.32):**

| תרחיש | חשבוניות | מחזור | סטטוס |
|--------|----------|-------|-------|
| ב' רגיל (jitter ×1.0) | 103 | 4,841 ₪ | ✅ |
| ד' רגיל (jitter ×1.2) | 139 | 6,533 ₪ | ✅ |
| א' + jitter ×1.3 | 168 | 7,896 ₪ | ✅ |
| א' + jitter ×1.5 | 193 | 9,071 ₪ | ✅ |
| יום משכורת (×1.25) + jitter ×1.3 | 210 | 9,870 ₪ | ✅ |
| שפל ימים 4-9 (×0.75) | 86 | 4,042 ₪ | ✅ נמוך אבל ריאליסטי |
| הפתעה חזק ×1.65 | 254 → **cap 260** | **12,220 ₪** | ✅ חריג מקסימלי |
| הפתעה חלש ×0.40 | 52 | 2,444 ₪ | ✅ יום שקט מאוד |

**סיכום טווח:** 2,400–12,200 ₪ — לגמרי ריאליסטי לחנות נוחות ישראלית.

---

### קוד לשינוי — 6 שורות בסה"כ

**קובץ 1: `02_extract_load_03.py`** — 2 שורות:
```python
# שינוי 1: הקטנת בסיס
SYNTHETIC_INVOICE_COUNT    = 85      # היה 105

# שינוי 2: האטת קצב הצמיחה
TREND_DAILY_GROWTH_RATE = 0.00015    # היה 0.0003
```

**קובץ 2: `generate_synthetic_invoices_03.py`** — 4 שורות:
```python
# שינוי 3+4: תיקון _surprise_event_multiplier
return w_rng.uniform(1.35, 1.65)    # היה (1.65, 2.50) — חזק
return w_rng.uniform(0.30, 0.52)    # היה (0.20, 0.45) — חלש

# שינוי 5: תיקון _monthly_event_multiplier (payday boost)
return rng.uniform(1.10, 1.30)      # היה (1.20, 1.45) — ימים 10-12

# שינוי 6: תקרה קשיחה ב-adjusted_invoice_count
return min(max(1, round(count)), 260)   # הוסף תקרה של 260 חשבוניות ליום
```
